apiVersion: v1
kind: Service
metadata:
  name: goodmorning
spec:
  type: ClusterIP
  selector:
    app: goodmorning
  ports:
    - name: http
      protocol: TCP
      port: "{{ .Values.service.port }}"
      targetPort: http
