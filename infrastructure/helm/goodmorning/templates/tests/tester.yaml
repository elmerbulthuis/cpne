apiVersion: v1
kind: Pod
metadata:
  name: goodmorning-tester
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget

      image: busybox
      command:
        - wget
      args: goodmorning:{{ .Values.service.port }}']
  restartPolicy: Never
